diff -urN framework_tool/build.rs framework_tool/build.rs
--- framework_tool/build.rs	2026-06-02 20:21:24.000000000 +0400
+++ framework_tool/build.rs	2026-06-29 12:10:10.963452583 +0400
@@ -1,23 +1,26 @@
 fn main() {
-    // Add app icon
-    if std::env::var_os("CARGO_CFG_WINDOWS").is_some() {
-        winresource::WindowsResource::new()
-            .set_icon("../res/framework_startmenuicon.ico")
-            .compile()
-            .unwrap();
-    }
+    #[cfg(windows)]
+    {
+        // Add app icon
+        if std::env::var_os("CARGO_CFG_WINDOWS").is_some() {
+            winresource::WindowsResource::new()
+                .set_icon("../res/framework_startmenuicon.ico")
+                .compile()
+                .unwrap();
+        }
 
-    let is_msvc = std::env::var("CARGO_CFG_TARGET_ENV")
-        .map(|v| v == "msvc")
-        .unwrap_or(false);
+        let is_msvc = std::env::var("CARGO_CFG_TARGET_ENV")
+            .map(|v| v == "msvc")
+            .unwrap_or(false);
 
-    if !cfg!(debug_assertions) && is_msvc {
-        // Statically link vcruntime to allow running on clean install
-        static_vcruntime::metabuild();
+        if !cfg!(debug_assertions) && is_msvc {
+            // Statically link vcruntime to allow running on clean install
+            static_vcruntime::metabuild();
 
-        // Embed resources file to force running as admin
-        embed_resource::compile("framework_tool-manifest.rc", embed_resource::NONE)
-            .manifest_optional()
-            .unwrap();
+            // Embed resources file to force running as admin
+            embed_resource::compile("framework_tool-manifest.rc", embed_resource::NONE)
+                .manifest_optional()
+                .unwrap();
+        }
     }
 }
diff -urN framework_tool/Cargo.toml framework_tool/Cargo.toml
--- framework_tool/Cargo.toml	2026-06-02 20:21:24.000000000 +0400
+++ framework_tool/Cargo.toml	2026-06-29 12:10:10.963417738 +0400
@@ -22,10 +22,6 @@
 path = "../framework_lib"
 version = "0.6.4"
 
-[build-dependencies]
-static_vcruntime = "3.0"
-embed-resource = "3.0"
-winresource = "0.1.17"
 
 [target.'cfg(windows)'.dependencies.winapi]
 version = "0.3.9"
@@ -33,6 +29,11 @@
   "wincon"
 ]
 
+[target.'cfg(windows)'.build-dependencies]
+static_vcruntime = "3.0"
+embed-resource = "3.0"
+winresource = "0.1.17"
+
 [package.metadata.winresource]
 LegalCopyright = "Framework Computer Inc © 2022"
 
